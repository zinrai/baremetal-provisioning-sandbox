---
- name: Ensure consul server configuration is set
  ansible.builtin.template:
    src: "etc/consul.d/server.hcl.j2"
    dest: "/etc/consul.d/server.hcl"
    owner: consul
    group: consul
    mode: "0644"
  notify:
    - reload consul
  tags:
    - consul_server_conf
