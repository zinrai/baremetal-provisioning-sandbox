# Install server environment for the baremetal-provisioning-sandbox.
# Rendered by Ansible (metal_install_conf); values come from the role defaults.

install_server:
  http_base: "{{ metal_install_conf.http_base }}"
